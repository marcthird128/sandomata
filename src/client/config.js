/* Sandomata Client
 * config.js
 * 
 * Contains default client config
 */

/** Default Config */
const config = {
    debug: false, // show debug logs
    allowLocalStorage: true, // allow saving to local storage
    embeddedAssets: false, // Are the assets in the html file
}

/* Export */
module.exports = { config };